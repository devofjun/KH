select * from dual;

-- create
create table tbl_test(
    usrid varchar2(15),
    score number
);

-- insert
insert into tbl_user (u_id, u_pw, u_name)
values ('test', '1234', 'test');

-- delete


-- select
